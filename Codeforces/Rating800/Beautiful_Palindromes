#include <stdio.h>
int main () {
    int t;
    while (t>0) {
        int n, k;
        scanf("%d %d", &n, &k);
        int arr[n];
        for (int i=0; i<n; i++) {
            scanf("%d", &arr[i]);
        }
        t--;
    }
}