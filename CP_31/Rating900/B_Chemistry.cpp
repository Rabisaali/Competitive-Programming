#include <bits/stdc++.h>
using namespace std;
#define LL long long int
int main () {
    LL t;
    cin >> t;
    while (t--) {
        LL n, k;
        cin >> n >> k;
        char arr[n];
        for (int i=0; i<n; i++) {
            cin >> arr[i];
        }
        int diff = n-k;
        if (diff%2 != 0) {
            
        } 
    }
}