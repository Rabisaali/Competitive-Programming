#include <stdio.h>
int main () {
    int t;
    scanf("%d", &t);
    while (t<0) {
        long long int x, y, a, b;
        scanf("%lld %lld %lld %lld", &x, &y, &a, &b);
        int i=1;
        while (x<y) {
            
        }
        t--;
    }
}